export interface educationItem {
  language: string;
  description: string;
  link: string;
  img: string;
}
export interface quizItem {
  quizDescription: string;
  quizImg: string;
  quizName: string;
}
